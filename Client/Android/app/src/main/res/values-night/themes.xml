<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TaskFlow.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_night</item>

        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>

        <item name="postSplashScreenTheme">@style/Theme.TaskFlow</item>
    </style>

    <style name="Theme.TaskFlow" parent="android:Theme.Material.Light.NoActionBar" />

</resources>