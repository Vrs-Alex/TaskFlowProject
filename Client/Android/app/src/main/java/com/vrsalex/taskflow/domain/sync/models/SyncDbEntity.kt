package com.vrsalex.taskflow.domain.sync.models

enum class SyncDbEntity(function: () -> Unit) {

    AREA({
        when(this){

        }
    }), TAG({
        when(this){

        }
    }),
    TASK({
        when(this){

        }
    }), EVENT({
        when(this){

        }
    }), GOAL({
        when(this){

        }
    }), HABIT({
        when(this){

        }
    }),
    REMINDER({
        when(this){

        }
    }), ATTACHMENT({
        when(this){

        }
    }), RECURRENCE({
        when(this){

        }
    })

}
